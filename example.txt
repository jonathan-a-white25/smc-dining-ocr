# Choose ONE method:

# (A) SendGrid (recommended for cloud)
SENDGRID_API_KEY=SG.xxxxxx

# (B) SMTP fallback (e.g., Gmail app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=youremail@example.com
SMTP_PASS=your_app_password
